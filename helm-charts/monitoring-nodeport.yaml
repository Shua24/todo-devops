grafana:
  service:
    type: NodePort
    nodePort: 32000  # choose an available port

prometheus:
  prometheusSpec:
    serviceMonitorSelector: {}
  service:
    type: NodePort
    nodePort: 32090  # choose an available port

